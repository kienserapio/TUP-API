# ── Local development ────────────────────────────────────────────
# Local Docker Postgres 17 + pgvector. See docs/15-local-development.md
DATABASE_URL=postgresql://postgres:dev@localhost:5432/tup
DATABASE_URL_POOLED=postgresql://postgres:dev@localhost:5432/tup
NODE_ENV=development
LOG_LEVEL=debug
PORT=3000

# 'fixtures' | 'live'. NEVER 'live' locally — docs/15 §1
FETCH_MODE=fixtures

# 'filesystem' | 'supabase'. Defaults to filesystem unless NODE_ENV=production.
# Explicit on purpose: deployment credentials in .env must never make a local run
# write snapshots into the production bucket.
SNAPSHOT_STORE=filesystem

# ── Production only — Fly secrets / GitHub encrypted secrets ─────
# DATABASE_URL=postgresql://...@...pooler.supabase.com:5432/postgres
# DATABASE_URL_POOLED=postgresql://...@...pooler.supabase.com:6543/postgres
# SUPABASE_URL=
# SUPABASE_SERVICE_ROLE_KEY=
# SENTRY_DSN=
# EMBEDDING_API_KEY=
